--- a/src/base/file_util.cc	2021-12-05 12:34:31.000000000 +0900
+++ b/src/base/file_util.cc	2021-12-09 17:36:35.000000000 +0900
@@ -40,7 +40,12 @@
 #include <unistd.h>
 #endif  // OS_WIN
 
+#if 1
+#include <stdlib.h>
+#include <string.h>
+#else
 #include <filesystem>
+#endif
 
 #include "base/file_stream.h"
 #include "base/logging.h"
@@ -490,6 +495,19 @@
 #ifdef __APPLE__
   return absl::UnimplementedError(
       "std::filesystem is only available on macOS 10.15, iOS 13.0, or later");
+#elif 1
+
+  const char *src = realpath(filename1.c_str(), NULL);
+  const char *dst = realpath(filename2.c_str(), NULL);
+  if (src == NULL || dst == NULL) {
+    free((void*)src);
+    free((void*)dst);
+    return false;
+  }
+  int s = strcmp(src, dst);
+  free((void*)src);
+  free((void*)dst);
+  return s == 0;
 #else   // __APPLE__
 
   // u8path is deprecated in C++20. The current target is C++17.
@@ -580,6 +598,9 @@
 #ifdef __APPLE__
   return absl::UnimplementedError(
       "std::filesystem is only available on macOS 10.15, iOS 13.0, or later.");
+#elif 1
+  return absl::UnimplementedError(
+      "Hardlink is not supported on android.");
 #else   // __APPLE__
 
   // u8path is deprecated in C++20. The current target is C++17.
